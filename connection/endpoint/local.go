// +build local

package endpoint

const (
	// BaseURL = "127.0.0.1:8371" // terminal

	BaseURL = "127.0.0.1:8000" // autorun
	// baseURL = "127.0.0.1:3500" // podm

  WebSocketScheme = "ws"
  PingScheme = "http"
)